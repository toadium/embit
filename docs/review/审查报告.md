# 审查报告

- 审查时间：2026-08-13
- 审查范围：01-项目申报书.md / 02-技术文档.md / 03-品牌故事.md / 04-README.md

## 问题清单

### P0（必须修复）
- [x] [02-技术文档.md] [第7.3节 配置说明表] 环境变量名前缀存在拼写错误：`EMBET_GAZEBO_ADDR` / `EMBET_VLA_BACKEND` / `EMBET_MODEL_PATH` / `EMBET_LOG_LEVEL` 全部缺少字母 `I`，应为 `EMBIT_GAZEBO_ADDR` / `EMBIT_VLA_BACKEND` / `EMBIT_MODEL_PATH` / `EMBIT_LOG_LEVEL`。 —— ✅ 已修复：将第7.3节配置说明表中所有 `EMBET_*` 环境变量名统一替换为 `EMBIT_*`。
- [x] [02-技术文档.md] [第7.3节 配置说明表] 环境变量 `EMBET_GAZEBO_ADDR` 的默认值填写为 `default`，与其他变量格式不一致，应明确填写实际默认地址（如 `localhost:9000`）或标注 `—`。 —— ✅ 已修复：将 `EMBIT_GAZEBO_ADDR` 的默认值从 `default` 改为 `localhost:9000`（Ignition Transport 默认地址）。

### P1（应该修复）
- [x] [01-项目申报书.md] [第1节 项目概述] 开头使用"全球首个完全基于 MoonBit 语言构建的通用具身智能机器人全栈开发框架"的绝对化表述，属于无法客观验证的绝对化用语，建议改为"基于 MoonBit 的通用具身智能机器人全栈开发框架"或补充可信来源。 —— ✅ 已修复：去掉"全球首个完全基于 MoonBit 语言构建的"，改为"基于 MoonBit 的通用具身智能机器人全栈开发框架"。
- [x] [04-README.md] [第"快速开始"→"安装"代码块] 功能清单中包含 `embit-examples` 模块，但快速开始安装步骤缺少 `moon add embit/embit-examples`，导致示例功能无法通过快速开始路径完成，建议补充。 —— ✅ 已修复：在安装步骤代码块末尾补充 `moon add embit/embit-examples`。
- [x] [03-品牌故事.md] [第1节 项目名称] 行内提示"⚠️ 建议上线前做商标检索"属于操作指引而非品牌故事正文内容，建议移至附录或实施计划的风险应对章节，保持品牌故事正文纯粹性。 —— ✅ 已修复：将该提示从第1节正文移出，新增附录"品牌风险备注"章节承载该操作指引。

> **阶段D 发现额外问题（P1）：02/03/04 三份文档仍保留相同绝对化表述"全球首个完全基于 MoonBit 语言构建的"，已在阶段C 第二轮修订中同步修复（与 01 保持一致）。**

### P2（建议优化）
- [x] [02-技术文档.md] [第5节 数据模型 核心实体表] `RobotModel` 实体中混入 `context_size`（上下文窗口大小）字段，该字段属于 VLA 推理模型概念，应归属 `VlaModel` 实体，当前 placement 会造成语义混淆。 —— ✅ 已修复：从 `RobotModel` 实体中移除 `context_size` 字段，将其移至 `VlaModel` 实体末尾。
- [x] [04-README.md] [第"快速开始"→"环境要求"] 仅列出 MoonBit 工具链和 Ignition Gazebo 两个主要依赖，缺少 CMake >= 3.20（FFI 编译必需）和 Git LFS（模型文件下载必需）的说明，建议与 02-技术文档第7.1节对齐。 —— ✅ 已修复：在环境要求中补充 `CMake >= 3.20（FFI 编译必需）` 和 `Git LFS（模型文件下载必需）` 两项说明。
- [x] [01-项目申报书.md] [第4节 产品亮点第4条] "实现微秒级控制环路抖动"中的"微秒级"为性能承诺，与第8节风险清单中的"VLA 模型推理延迟难以满足实时控制需求"存在逻辑张力，建议在实施计划中补充阶段性性能验证里程碑。 —— ✅ 已修复：在 Phase 4（第5个月）任务描述中补充"完成控制环路性能验证（延迟基准测试、抖动测量）"。

## 审查结论
- 是否通过：**通过**（所有 P0 问题已修复，P1/P2 问题均已处理）
